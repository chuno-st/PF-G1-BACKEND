const { Router } = require("express");
// const { check } = require("express-validator");
// const {paymentProduct} = require('../controllers/products')
const router = Router();

// router.post('/notificaciones',paymentProduct)

module.exports = router